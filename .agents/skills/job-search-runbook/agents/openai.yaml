interface:
  display_name: "Job Search Runbook"
  short_description: "Run the candidate job-search workflow."
  default_prompt: "Use $job-search-runbook to build the search brief first, derive the query plan from career/profile/cv_plain.txt plus career/profile/profile.yaml, discover through official or public sources, verify direct apply links, and update the search state."

policy:
  allow_implicit_invocation: true
